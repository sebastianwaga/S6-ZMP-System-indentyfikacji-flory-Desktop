<Window x:Class="VirtualHerbarium.AdminPanel.Views.CreateEditHerbariumView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource HerbariumDialog_Title}"
        Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" Margin="0 0 0 15"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="{DynamicResource HerbariumDialog_Name}"/>
            <TextBox Text="{Binding Name}" Margin="0 5 0 10"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="{DynamicResource HerbariumDialog_Description}"/>
            <TextBox Text="{Binding Description}" Margin="0 5 0 10"/>
        </StackPanel>

        <StackPanel Grid.Row="3">
            <CheckBox Content="{DynamicResource HerbariumDialog_Public}" IsChecked="{Binding IsPublic}" Margin="0 0 0 10"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{DynamicResource HerbariumDialog_Cancel}" Width="80" Margin="0 0 10 0" Click="Cancel_Click"/>
                <Button Content="{DynamicResource HerbariumDialog_Save}" Width="80" Click="Save_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
