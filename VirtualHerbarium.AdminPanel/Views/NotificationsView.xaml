<UserControl x:Class="VirtualHerbarium.AdminPanel.Views.NotificationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<Grid Margin="20">

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<StackPanel Orientation="Horizontal" Margin="0 0 10 0">

			<Button Content="Odśwież"
                    Width="150"
                    Command="{Binding RefreshCommand}"/>

			<Button Content="Oznacz wszystkie jako przeczytane"
                    Width="250"
                    Command="{Binding MarkAllCommand}"/>
		</StackPanel>

		<ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0 20 0 0">
			<ItemsControl ItemsSource="{Binding Notifications}">
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<Border BorderBrush="#DDD"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="12"
                                Margin="0 0 0 12"
                                Background="White">

							<StackPanel>

								<TextBlock Text="{Binding title}"
                                           FontSize="16"
                                           FontWeight="Bold"/>

								<TextBlock Text="{Binding message}"
                                           TextWrapping="Wrap"
                                           Margin="0 6 0 6"/>

								<TextBlock Text="{Binding createdAt}"
                                           FontStyle="Italic"
                                           Foreground="Gray"/>

								<Button Content="Oznacz jako przeczytane"
                                        Width="200"
                                        Margin="0 10 0 0"
                                        Command="{Binding DataContext.MarkReadCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>

							</StackPanel>

						</Border>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</ScrollViewer>

	</Grid>
</UserControl>
