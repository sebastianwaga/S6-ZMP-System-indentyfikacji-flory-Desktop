<UserControl x:Class="VirtualHerbarium.AdminPanel.Views.NotificationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="20">

        <Border Background="White"
                CornerRadius="12"
                Padding="16"
                SnapsToDevicePixels="True">

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{DynamicResource Notifications_Title}"
                           FontSize="18"
                           FontWeight="Bold"
                           Margin="0,0,0,12"/>

                <StackPanel Grid.Row="1" Margin="0,0,0,12">
                    <TextBlock Text="{DynamicResource Notifications_Label_Title}" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                             Height="32"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,0,0,12">
                    <TextBlock Text="{DynamicResource Notifications_Label_Message}" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Height="150"
                             TextWrapping="Wrap"/>
                </StackPanel>

                <Button Grid.Row="3"
                        Content="{DynamicResource Notifications_Button_Send}"
                        HorizontalAlignment="Right"
                        Width="200"
                        Height="36"
                        Background="#1976D2"
                        Foreground="White"
                        BorderThickness="0"
                        Command="{Binding SendCommand}"/>
            </Grid>

        </Border>

    </Grid>
</UserControl>
