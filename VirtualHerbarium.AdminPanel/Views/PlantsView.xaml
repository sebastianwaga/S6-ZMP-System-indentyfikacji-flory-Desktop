<UserControl x:Class="VirtualHerbarium.AdminPanel.Views.PlantsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="20">
        <Border Background="White"
                CornerRadius="12"
                Padding="16"
                SnapsToDevicePixels="True">

            <DataGrid ItemsSource="{Binding Plants}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      RowHeight="40"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nazwa" Binding="{Binding name}" Width="*" />
                    <DataGridTextColumn Header="Gatunek" Binding="{Binding species}" Width="*" />
                    <DataGridTextColumn Header="Właściciel" Binding="{Binding owner}" Width="*" />
                    <DataGridCheckBoxColumn Header="Zweryfikowana" Binding="{Binding verified}" Width="120" />

                    <DataGridTemplateColumn Header="Akcje" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Usuń"
                                        Background="#FFD32F2F"
                                        Foreground="White"
                                        Padding="8 4"
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
