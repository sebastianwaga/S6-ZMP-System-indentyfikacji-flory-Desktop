<UserControl x:Class="VirtualHerbarium.AdminPanel.Views.PlantsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="{DynamicResource Plants_Add}"
                Background="#4CAF50"
                Foreground="White"
                Padding="10 6"
                Margin="0 0 0 10"
                HorizontalAlignment="Left"
                Command="{Binding AddPlantCommand}" />

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto">

            <Border Background="White"
                    CornerRadius="12"
                    Padding="16">

                <DataGrid ItemsSource="{Binding Plants}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          RowHeight="40"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#388E3C"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="8"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.Columns>

                        <DataGridTextColumn Header="{DynamicResource Plants_Header_Name}"
                                            Binding="{Binding name}"
                                            Width="200" />

                        <DataGridTemplateColumn Header="{DynamicResource Plants_Header_Actions}" Width="350">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">

                                        <Button Content="{DynamicResource Plants_Action_Details}"
                                                Background="#0288D1"
                                                Foreground="White"
                                                Padding="8 4"
                                                Margin="0 0 6 0"
                                                Command="{Binding DataContext.ShowPlantDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}" />

                                        <Button Content="{DynamicResource Plants_Action_Edit}"
                                                Background="#1976D2"
                                                Foreground="White"
                                                Padding="8 4"
                                                Margin="0 0 6 0"
                                                Command="{Binding DataContext.EditPlantNameCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}" />

                                        <Button Content="{DynamicResource Plants_Action_Delete}"
                                                Background="#D32F2F"
                                                Foreground="White"
                                                Padding="8 4"
                                                Command="{Binding DataContext.DeletePlantCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}" />

                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>

                </DataGrid>
            </Border>
        </ScrollViewer>

    </Grid>
</UserControl>
